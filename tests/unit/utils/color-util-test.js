import colorUtil from 'redux-test/utils/color-util';
import { module, test } from 'qunit';

module('Unit | Utility | color util');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = colorUtil();
  assert.ok(result);
});
